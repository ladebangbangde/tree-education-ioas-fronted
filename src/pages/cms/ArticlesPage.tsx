import { Button, Form, Input, Select, Space, Tag } from 'antd';import { useNavigate } from 'react-router-dom';
import { DataTable, PageHeader, SearchFilterBar } from '@/components';import { articleCategories, articles } from '@/mock/cms';import { useEnterpriseActions } from '@/hooks/useEnterpriseActions';
export default function ArticlesPage(){const nav=useNavigate();const {openAction,contextHolder}=useEnterpriseActions('文章管理');const columns=[{title:'标题',dataIndex:'title'},{title:'分类',dataIndex:'category'},{title:'作者',dataIndex:'author'},{title:'状态',dataIndex:'status',render:(v:string)=><Tag color={v==='已发布'?'green':v==='草稿'?'default':'blue'}>{v}</Tag>},{title:'发布时间',dataIndex:'publishAt'},{title:'SEO',dataIndex:'seo'},{title:'PV',dataIndex:'pv'},{title:'操作',render:(_:unknown,r:any)=> <Space><Button type='link' onClick={()=>nav(`/cms/article/edit/${r.id}`)}>编辑</Button><Button type='link' onClick={()=>nav(`/cms/article/preview/${r.id}`)}>预览</Button><Button type='link' onClick={()=>openAction('publish',r)}>发布</Button><Button type='link' danger onClick={()=>openAction('offline',r)}>下线</Button><Button type='link' onClick={()=>openAction('log',r)}>日志</Button></Space>}];return <>{contextHolder}<PageHeader title='CMS / 文章管理' extra={<Space><Button onClick={()=>nav('/cms/config/home')}>首页模块配置</Button><Button onClick={()=>nav('/cms/config/seo')}>SEO配置</Button><Button type='primary' onClick={()=>nav('/cms/article/edit/A01')}>新建文章</Button></Space>} /><SearchFilterBar><Form layout='inline'><Form.Item label='分类'><Select style={{width:160}} options={articleCategories.map(i=>({value:i}))}/></Form.Item><Form.Item label='关键词'><Input placeholder='输入标题关键词'/></Form.Item><Space><Button type='primary'>筛选</Button><Button>重置</Button></Space></Form></SearchFilterBar><DataTable rowKey='id' columns={columns} dataSource={articles} /></>}
