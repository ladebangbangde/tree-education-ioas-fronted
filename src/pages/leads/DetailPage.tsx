import { Button, Card, Col, Descriptions, Row, Tabs } from 'antd';import { useParams } from 'react-router-dom';
import { LeadTimeline, PageHeader, StatusTag } from '@/components';import { followLogs, leadsList } from '@/mock/leads';
export default function LeadsDetailPage(){const {id}=useParams();const lead=leadsList.find(i=>i.id===id)||leadsList[0];
return <><PageHeader title='线索详情' extra={<Button type='primary'>记录跟进</Button>}/><Row gutter={12}><Col span={18}><Card><Descriptions column={2} items={[{label:'学生姓名',children:lead.studentName},{label:'线索编号',children:lead.id},{label:'状态',children:<StatusTag status={lead.status}/>},{label:'顾问',children:lead.advisor},{label:'联系电话',children:lead.phone},{label:'来源',children:lead.source}]}/><Tabs items={[{key:'1',label:'线索概览',children:<Descriptions column={2} items={[{label:'意向国家',children:lead.intentCountry},{label:'学历背景',children:lead.degree},{label:'创建时间',children:lead.createdAt}]} />},{key:'2',label:'跟进记录',children:<LeadTimeline items={followLogs}/>},{key:'3',label:'资料文件',children:'护照、成绩单、语言成绩（Mock）'},{key:'4',label:'操作日志',children:'2026-05-05 系统分配顾问'}]}/></Card></Col><Col span={6}><Card title='快捷操作'><Button block>分配顾问</Button><Button block className='mt12'>转学生档案</Button><Button block className='mt12'>标记高意向</Button></Card></Col></Row></>}
