import { Checkbox } from 'antd';import { DataTable, PageHeader, StatusTag } from '@/components';import { materials } from '@/mock/applications';
export default function MaterialsPage(){const columns=[{title:'材料名称',dataIndex:'name'},{title:'状态',dataIndex:'status',render:(v:string)=><StatusTag status={v}/>},{title:'截止时间',dataIndex:'deadline'},{title:'负责人',dataIndex:'owner'},{title:'备注',dataIndex:'remark'},{title:'完成',render:()=> <Checkbox/>}];return <><PageHeader title='材料清单'/><DataTable rowKey='id' columns={columns} dataSource={materials} pagination={false}/></>}
