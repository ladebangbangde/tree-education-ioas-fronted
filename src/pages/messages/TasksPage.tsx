import { Tabs } from 'antd';import { DataTable, PageHeader, StatusTag } from '@/components';import { tasks } from '@/mock/tasks';
export default function TasksPage(){const columns=[{title:'任务内容',dataIndex:'task'},{title:'优先级',dataIndex:'priority'},{title:'截止时间',dataIndex:'deadline'},{title:'任务状态',dataIndex:'status',render:(v:string)=><StatusTag status={v}/> }];
return <><PageHeader title='任务中心'/><Tabs items={[{key:'todo',label:'待办任务',children:<DataTable rowKey='task' columns={columns} dataSource={tasks.todo} pagination={false}/>},{key:'done',label:'已办任务',children:<DataTable rowKey='task' columns={columns} dataSource={tasks.done} pagination={false}/>},{key:'timeout',label:'超时任务',children:<DataTable rowKey='task' columns={columns} dataSource={tasks.timeout} pagination={false}/>}]} /></>}
