import { Button, Card, Checkbox, Form, Input, Select, Typography } from 'antd';import { useLocation, useNavigate } from 'react-router-dom';import { defaultRouteByRole, roleLabels, roles } from '@/constants/permissions';import { useAuthStore } from '@/store/auth';import type { Role } from '@/types';
export default function LoginPage(){const nav=useNavigate();const loc=useLocation();const login=useAuthStore(s=>s.login);return <div className='login'><div className='login-left'><div><h1>留学中介运营中台</h1><p>覆盖线索、学生、申请、内容、报表全链路</p></div></div><div className='login-right'><Card className='login-card'><Typography.Title level={3}>欢迎登录</Typography.Title><Form layout='vertical' initialValues={{username:'运营管理员',role:'OPERATOR',remember:true}} onFinish={(v)=>{const role=v.role as Role;login(v.username,v.password,role);nav((loc.state as any)?.from||defaultRouteByRole[role]);}}><Form.Item label='账号' name='username' rules={[{required:true,message:'请输入账号'}]}><Input/></Form.Item><Form.Item label='密码' name='password' rules={[{required:true,message:'请输入密码'}]}><Input.Password/></Form.Item><Form.Item label='后台角色' name='role' rules={[{required:true,message:'请选择角色'}]}><Select options={roles.filter(i=>i!=='SUPER_ADMIN'||true).map(value=>({value,label:`${value} · ${roleLabels[value]}`}))}/></Form.Item><Form.Item name='remember' valuePropName='checked'><Checkbox>记住我</Checkbox></Form.Item><Button type='primary' htmlType='submit' block>登录系统</Button></Form></Card></div></div>}
