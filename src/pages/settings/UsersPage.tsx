import { Button, Form, Input, Modal, Switch } from 'antd';import { useState } from 'react';import { DataTable, PageHeader } from '@/components/common';
export default function(){const [open,setOpen]=useState(false);const cols=[{title:'姓名',dataIndex:'name'},{title:'角色',dataIndex:'role'},{title:'部门',dataIndex:'dept'},{title:'状态',render:()=> <Switch defaultChecked/>}];return <><PageHeader title='用户管理' extra={<Button type='primary' onClick={()=>setOpen(true)}>新建用户</Button>}/><DataTable rowKey='key' columns={cols} dataSource={[{key:'1',name:'Amy',role:'咨询顾问',dept:'咨询部'}]} /><Modal open={open} onCancel={()=>setOpen(false)} onOk={()=>setOpen(false)} title='新建用户'><Form layout='vertical'><Form.Item label='姓名'><Input/></Form.Item></Form></Modal></>}
