import { BellOutlined, LogoutOutlined, SearchOutlined, UserOutlined } from '@ant-design/icons';import { Avatar, Badge, Dropdown, Input, Layout, Space, Tag } from 'antd';import { useNavigate } from 'react-router-dom';import { useAuthStore } from '@/store/auth';
const { Header } = Layout;
export default function AppHeader(){const nav=useNavigate();const {userName,role,logout}=useAuthStore();return <Header className='top'><Space size={16}><Input prefix={<SearchOutlined/>} placeholder='搜索学生 / 线索 / 任务' style={{width:300}}/><Badge count={6}><BellOutlined className='icon-btn'/></Badge><Tag color='blue'>{role}</Tag><Dropdown menu={{items:[{key:'logout',icon:<LogoutOutlined/>,label:'退出登录',onClick:()=>{logout();nav('/login');}}]}}><Space className='click'><Avatar icon={<UserOutlined/>}/>{userName}</Space></Dropdown></Space></Header>}
